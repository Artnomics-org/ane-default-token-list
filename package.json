{
  "name": "@art-economics/default-token-list",
  "version": "1.1.10",
  "description": "The Art Economics default token list",
  "main": "build/ane-default.tokenlist.json",
  "scripts": {
    "test": "mocha",
    "build": "rimraf build && mkdir build && node src/write.js > build/ane-default.tokenlist.json",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "build"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Artnomics-org/ane-default-token-list.git"
  },
  "keywords": [
    "uniswap",
    "default",
    "token",
    "list"
  ],
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@ethersproject/address": "^5.0.2",
    "@uniswap/token-lists": "^1.0.0-beta.8",
    "ajv": "^6.12.3",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "rimraf": "^3.0.2"
  }
}
